## Create the project directory
```
mkdir -p ~/dev/etherium-dbt
cd ~/dev/etherium-dbt
```

## Make the venv

    python -m venv venv
    source venv/bin/activate

## Install dbt in venv
    python -m pip install dbt-oracle

    pip install dbt-snowflake
    pip list
    dbt --version

GBGJTYS-DG86643.snowflakecomputing.com